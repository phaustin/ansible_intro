---
- hosts: all
  vars_files:
    - vars/default.yml
  tasks:
    - name: write docker-compose.yml for dashboards
      template:
        src: '{{ item.src }}'
        dest: '{{ item.dest }}'
      loop:
        "{{ dashboard_template_list }}"


